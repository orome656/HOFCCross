<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:HOFCCross.Controls"
             x:Class="HOFCCross.Page.ActuPage"
             Title="Actualités">
  <ContentPage.Content>
    <ListView x:Name="listActus" ItemsSource="{Binding Actus}" HasUnevenRows="True" SeparatorVisibility="None">
      <ListView.ItemTemplate>
        <DataTemplate>
          <controls:CardView Title="{Binding Title}" Detail="{Binding Detail}" ImageUrl="{Binding ImageUrl}" Date="{Binding Date}"/>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </ContentPage.Content>
</ContentPage>